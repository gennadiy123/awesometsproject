import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  modal: {
    margin: 20,
    marginTop: 200,
    width: 400,
    height: 100,
    backgroundColor: 'white',
    borderRadius: 20,
    alignItems: 'center',
  },

  textInput: {
    borderRadius: 1,
    borderWidth: 1
  }
});
